# NPM Registry Configuration for Console Log Pipe
# This file configures npm to use the appropriate registries for different scopes

# Default registry (for dependencies)
registry=https://registry.npmjs.org/

# NPM registry for @kansnpms scope (public packages)
@kansnpms:registry=https://registry.npmjs.org/

# Always authenticate when publishing
always-auth=true
